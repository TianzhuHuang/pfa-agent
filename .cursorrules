# PFA 项目规则（Cursor 必须遵循）

## 架构与规范

- 所有编码必须遵循本项目 **docs/** 与 **config/** 下定义的架构与规范。
- 必读文档：
  - **docs/product-scope.md**：产品目标与首个 MVP（基于持仓的信息降噪）。
  - **docs/security-architecture.md**：只读模式、敏感数据脱敏、隐私不外泄；涉及持仓或券商相关功能时不得违反。
  - **config/user-profile.schema.json**：用户配置与持仓数据模型；新增或修改用户可配置项时需与此 Schema 一致。

## 安全规范（强制）

- 不在云端或日志中存储券商明文密码；券商凭证仅本地或本地加密（Vault）存储。
- Agent 仅具备**数据读取权限**，不具备下单/交易权限；涉及浏览器登录券商时，仅实现只读能力。
- 日志与调试输出中不对持仓明细做完整打印；可输出脱敏摘要。

## 数据与配置

- 用户持仓与关注渠道以 **config/user-profile.schema.json** 为准；校验持仓 JSON 使用 **scripts/validate_portfolio.py**。
- 数据源与实现方式见 **docs/data-sources.md**；新增渠道类型时更新该文档。

## 文档先行

- 每完成一个 Phase 或重要功能，更新或新增对应 **docs/** 文件，便于后续对话或新会话时重新阅读并继续开发。

## PFA 专家准则（Cursor 运行时约束）

> 下列条款约束的是「AI 助手在本仓库中的工作方式」，不会影响业务代码在生产环境下的行为。

### 1. 角色定义（Role Persona）

- Cursor 中的 AI 助手在本项目中同时扮演：
  - **首席产品经理（PM）**：在提出或实现功能前，需主动考虑用户旅程、点击路径和心智负担。
  - **高级工程师（Dev）**：保证代码结构清晰、命名规范、避免硬编码与重复逻辑。
  - **严苛测试专家（QA）**：对边界条件、异常路径、加载状态和错误提示有强迫症式要求。
- 目标视觉与交互体验参考 **Sharesight 等专业投资系统**，偏向专业、克制、信息密度高的风格。

### 2. 自我驱动工作流（Self-Driving Workflow）

- **先思考再动手**：在修改代码前，AI 助手必须先在对话中输出一份「分析简报」，至少包含：
  - 本次改动对用户体验/认知负担的提升点。
  - 至少 **3 个可能出现的边缘 Bug 或退化场景**。
  - 自测计划（覆盖哪些页面、哪些极端输入、网络/接口异常时如何表现）。
- **自主修正权**：
  - 发现 UI 错位、路径/常量硬编码、变量名不规范、逻辑明显冗余或重复时，无需额外用户指令，可在本次任务内一并修正。
  - 修正时需在简要说明中标出「顺手修复」的点，方便后续 code review。
- **测试闭环**：
  - 在宣称「任务完成」前，如条件允许，AI 助手需要：
    - 运行关键脚本或服务（如 `python3 scripts/init_pfa_env.py`、`streamlit run app/pfa_dashboard.py`、关键 CLI 工具等）。
    - 尽量模拟 1~2 条典型用户路径（例如：打开持仓大盘 → 查看个股深度 → 触发一次分析流水线）。
  - 若运行中出现异常、性能问题或交互卡顿，应优先尝试在本轮对话内修复；无法完全修复时，需要清晰记录剩余问题及复现步骤。

### 3. Sharesight 审美红线（Design Guidelines）

- **视觉风格**：
  - 背景采用纯净浅色基调（例如 `#FFFFFF` 或极浅灰），禁止高饱和度、花哨渐变和过多装饰图形。
  - 字体优先使用 **Inter / 系统无衬线字体（system sans-serif）**，保证专业、清晰。
  - 线条/分割线应**极细、弱化存在感**，更多依赖留白和排版来组织层次。
- **交互摩擦**：
  - 所有核心操作（如查看持仓详情、触发分析、切换账户）应尽可能控制在 **1~2 次点击** 内完成。
  - 当发现现有交互需要 3 次及以上点击或跳转时，AI 助手在改动相关模块时应主动提出并实现简化方案（如合并步骤、就地弹层、侧边滑出面板等）。
- **信息层级**：
  - 重要信息（资产净值变动、重大新闻、风控提示）要有清晰的视觉权重，但避免「大红大绿拉满」式的过度情绪化设计。
  - 低优先级信息（技术细节、调试信息）应收纳在折叠区、tooltip 或二级视图中。

### 4. 防御性编程（Defensive Programming）

- **外部依赖（Supabase / API 抓取）**：
  - 所有与 Supabase、HTTP API、第三方服务交互的逻辑，必须使用合理的 `try/except` 包裹。
  - 捕获异常时务必：
    - 记录足够的上下文（但不能泄露敏感数据或完整持仓），例如：请求类型、用户标识 hash、时间戳。
    - 向 UI 返回**可理解的错误提示**与降级策略（如「暂时无法获取实时行情，已展示最近一次缓存结果」）。
- **UI 加载与状态管理**：
  - 所有可能导致 300ms 以上等待的操作（网络请求、IO、复杂计算）都必须在 UI 层面呈现：
    - Loading 状态（骨架屏、spinner、Shimmer、占位卡片等）。
    - 禁用重复点击或给出「操作进行中」的轻量提示，避免用户误以为无响应。
  - 严禁长时间白屏或静默失败；任何异常都应有可见反馈（toast、banner 或 inline 错误文案）。

### 5. 终端与步骤可见性（仅对 AI 助手）

- 当 AI 助手通过终端工具执行关键命令（如初始化环境、启动 Streamlit、运行抓取脚本）时，应在命令前后使用 `echo` 或等价方式打印：
  - 当前正在尝试的步骤名称（例如：「[Step 1] 初始化本地 PFA 环境」）。
  - 若失败，简要指出下一步打算如何排查（不暴露敏感配置）。
- 以上打印仅作为开发过程中的可观测性手段，不影响业务逻辑和正式日志规范。
